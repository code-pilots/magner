const enCustom = {
  home: {
    hello: 'Hello from the universal admin!',
    custom: 'This is a fully custom page. And you can make your own too!',
    explore: 'Explore table',
  },
};

/** !Don't change this! */
export type CustomTranslationSchema = typeof enCustom;
export default enCustom;
