/* eslint-disable no-shadow, no-unused-vars */

/**
 * It is required to fill this file with at least one ROLE that will be used for
 * restricting the access to routes.
 *
 * Don't rename the file or the variable ROLE name!
 */
enum ROLE {
  ADMIN = 'ROLE_ADMINISTRATOR',
  SUPER_ADMIN = 'ROLE_SUPER_ADMIN',
  DOCTOR = 'ROLE_DOCTOR',
}

export default ROLE;
